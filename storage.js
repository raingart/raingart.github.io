const Storage = function () {
   let saveParams = {};

   return {
      setParams(x = required(), sync_type) {
         
      },

      getParams(callback, sync_type, x) {
         
      },
   }
}();
